services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    hostname: personal-podcast-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./src:/var/www/html
      - ./src/storage:/var/www/html/storage
    networks:
      - podcast-network

  webserver:
    image: nginx:alpine
    container_name: podcast-feed-webserver
    restart: unless-stopped
    networks:
      - traefik_web
      - podcast-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.podcast.rule=Host(`yourdomain.com`)"
      - "traefik.http.routers.podcast.entrypoints=websecure"
      - "traefik.http.routers.podcast.tls.certresolver=yourresolver"
      - "traefik.http.services.podcast.loadbalancer.server.port=80"
    depends_on:
      - app
    volumes:
      - ./src:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf

networks:
  traefik_web:
    external: true
  podcast-network:
    driver: bridge
